---
services:
  uptime-kuma-agent-mcp:
    build:
      context: .. # Production
      dockerfile: docker/debug.Dockerfile # Using debug for local dev usually
    container_name: uptime-kuma-agent-mcp
    hostname: uptime-kuma-agent-mcp
    command: [ "uptime-kuma-agent-mcp" ]
    extra_hosts:
      - "host.docker.internal:host-gateway"
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    restart: always
    volumes:
      - ../mcp:/app
    env_file:
      - ../.env
    environment:
      - "PYTHONUNBUFFERED=1"
      - "HOST=0.0.0.0"
      - "PORT=8004"
      - "TRANSPORT=streamable-http"
    ports:
      - "8004:8004"
    healthcheck:
      test: [ "CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8004/health')" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

  uptime-kuma-agent-agent:
    build:
      context: .. # Production
      dockerfile: docker/debug.Dockerfile
    container_name: uptime-kuma-agent-agent
    hostname: uptime-kuma-agent-agent
    command: [ "uptime-kuma-agent-agent" ]
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      - uptime-kuma-agent-mcp
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    restart: always
    env_file:
      - ../.env
    environment:
      - "PYTHONUNBUFFERED=1"
      - "HOST=0.0.0.0"
      - "PORT=9004"
      - "MCP_URL=http://uptime-kuma-agent-mcp:8004/mcp"
      - "PROVIDER=openai"
      - "LLM_BASE_URL=${LLM_BASE_URL:-http://host.docker.internal:1234/v1}"
      - "LLM_API_KEY=${LLM_API_KEY:-llama}"
      - "MODEL_ID=${MODEL_ID:-nvidia/nemotron-3-super}"
      - "DEBUG=True"
      - "ENABLE_WEB_UI=True"
      - "ENABLE_OTEL=True"
    ports:
      - "9004:9004"
    healthcheck:
      test: [ "CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:9004/health')" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
